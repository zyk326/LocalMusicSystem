package util.frame;

import util.GetDefaultData;

import javax.swing.*;
import javax.swing.border.Border;
import javax.swing.border.EtchedBorder;
import java.awt.*;

/**
 * 创建一个面板
 */
public class MainPanel {
    public static void main(String[] args) {
    }

    /**
     * 初始化一个面板
     */
    public void createPannel(String type, Panel panel){
        //Border border = new EtchedBorder(EtchedBorder.RAISED);


        //获取面板坐标
//        int Top_Left = Integer.parseInt(GetDefaultData.getTop_Left(type));
//        int Top_Right = Integer.parseInt(GetDefaultData.getTop_Right(type));
//        int Ground_Left = Integer.parseInt(GetDefaultData.getGround_Left(type));
//        int Ground_Right = Integer.parseInt(GetDefaultData.getGround_Right(type));
//        //设置面板坐标
//        panel.setBounds(Top_Left, Top_Right, Ground_Left, Ground_Right);
//
//        System.out.println(type + "    :   " + Top_Left + "  " + Top_Right + "  " + Ground_Left + "  " + Ground_Right);

    }
}
